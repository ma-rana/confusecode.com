// This component has been removed. Uploading a file now opens it in its own
// tab (each file keeps its own independent work), so the mid-session "you'll
// lose progress" confirmation is no longer needed. Safe to delete this file.
export {};
