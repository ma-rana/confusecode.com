# Backend configuration (all optional — sane defaults in src/config.ts)
PORT=4000
HOST=127.0.0.1
# Origin allowed to call the API. In dev this is the Next.js dev server.
ALLOWED_ORIGIN=http://localhost:3000
