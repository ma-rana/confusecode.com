{
  "name": "confusecode-backend",
  "version": "0.1.0",
  "description": "ConfuseCode backend — static analysis API (JS/TS via ESLint). Parse-only, stateless, stores nothing.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "node --test dist/*.test.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^9.17.0",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
